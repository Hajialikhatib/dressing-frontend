services:
  - type: web
    name: dressing-frontend
    env: static
    rootDir: Desktop/dressing-frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
